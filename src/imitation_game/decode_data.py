def decode_data(encoded_message, shift):
    """
    Restores an encoded string to its original form using a shift value.

    Parameters
    ----------
    encoded_message : str
        The string previously processed by `encode_data`.
    shift : int
        The numerical shift value used during the initial encoding.

    Returns
    -------
    str
        The original human-readable message.
    """
    pass