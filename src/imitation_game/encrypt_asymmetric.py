def encrypt_asymmetric(message, public_key):
    """
    Encrypts a message using an RSA public key.

    Parameters
    ----------
    message : str
        The plaintext message to be secured.
    public_key : str
        The recipient's public key.

    Returns
    -------
    str
        The encrypted ciphertext, decryptable only by the corresponding 
        private key.
    """
    pass